export interface UserInterface {
  userId: string;
  userName: string;
  userImage: string;
  onlineStatus: boolean;
  lastSeen: string;
  createdAt: string;
}
export interface UserShortInterface {
  userId: string;
  userImage: string;
  userName: string;
  onlineStatus: boolean;
}
